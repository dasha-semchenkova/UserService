spring:
  datasource:
    url: jdbc:postgresql://user-service-postgres:5432/testdb
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      connection-timeout: 30000
      initialization-fail-timeout: 0
      max-lifetime: 30000

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false
    defer-datasource-initialization: true

  redis:
    host: localhost
    port: 6379
    timeout: 5000

  test:
    database:
      replace: none